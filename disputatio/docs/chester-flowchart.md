# 체셔는 어떻게 작동하나요?

체셔는 게임 속 저택에 사는 **앵무새 NPC**입니다.
플레이어가 퀴즈를 풀 때 힌트를 주는 역할을 하며,
플레이어가 얼마나 잘하고 있는지를 스스로 판단해서 **힌트의 양을 자동으로 조절**합니다.

---

## 전체 흐름 한눈에 보기

```mermaid
flowchart TD
    subgraph STEP1["① 체셔와 대화 시작하기"]
        A["🖱️ 플레이어가\n체셔 창문을 클릭"] --> B{앵무새도\n클릭했나요?}
        B -- 네 --> C["🦜 앵무새 클릭!\n바로 퀴즈 질문을 시작합니다"]
        B -- 아니요 --> D["💬 창문 열기만 한 상태\n인사 후 퀴즈 질문으로 넘어갑니다"]
    end

    subgraph STEP2["② 체셔가 무슨 말을 할지 결정하기"]
        C --> E["❓ 군더더기 없이\n퀴즈 질문 한 줄만 말하기"]
        D --> F["👋 짧게 인사한 뒤\n퀴즈 질문 말하기"]
        G["📝 플레이어가\n답을 입력함"] --> H{답변을\n채점할까요?}
        H -- 네 --> I["🔍 채점 서버로 전송\n정답 여부 확인"]
        H -- 아니요\n타이머 알림 등 --> J["📢 그대로 AI에게 전달"]
    end

    subgraph STEP3["③ 체셔의 말투와 성격 준비하기"]
        E & F & J --> K["📜 체셔의 기본 정체성 설정\n저택의 앵무새, 퀴즈 출제 규칙"]
        K --> L["🎭 체셔 특유의 말투 규칙 추가\n공통 말투 파일 적용"]
        L --> M["🔎 플레이어 행동 기록 확인\n이 방에 몇 번 왔는지, 얼마나 자주 왔는지"]
    end

    subgraph STEP4["④ 힌트를 얼마나 줄지 계산하기"]
        M --> N["📊 플레이어 실력 점수 계산"]
        N --> O["🧱 막힘 점수 계산\n같은 방을 자꾸 왔다 갔다 하면 높아짐\n짧은 간격으로 반복 방문하면 높아짐\n와도 진전이 없으면 높아짐"]
        N --> P["🏆 종합 실력 점수 계산\n진행도 + 정답률 − 막힘 정도"]
        P --> Q{실력 점수 판정}
        Q -- "낮음\n많이 막힌 상태" --> R["🟥 초보 플레이어\n더 직접적인 힌트 제공"]
        Q -- "보통" --> S["🟨 중간 플레이어\n방향만 살짝 알려주기"]
        Q -- "높음\n잘 풀고 있는 상태" --> T["🟩 숙련 플레이어\n거의 힌트 없이 미스터리 유지"]
    end

    subgraph STEP5["⑤ AI에게 지시하고 대답 받기"]
        R --> U["📨 완성된 지시문을\nAI에게 전달\n실력에 맞는 힌트 수준 포함"]
        S --> U
        T --> U
        U --> V["⏳ AI가 생각 중...\n3초 이상 걸리면\n'잠깐 생각하는 중' 말풍선 표시"]
        V --> W["💬 체셔가 대사를\n말풍선으로 출력"]
    end

    subgraph STEP6["⑥ 채점 결과 처리하기"]
        I --> X["✅ 정답 / ❌ 오답 판정"]
        X -- 정답 --> Y["🎉 정답 반응\n다음 문제로 넘어가기"]
        X -- 오답 --> Z["💡 틀렸을 때 반응\n다시 도전할 수 있도록 안내"]
        W --> AA{5문제를\n모두 풀었나요?}
        Y --> AA
        AA -- 네 --> AB["🏁 퀴즈 완료!\n입력창 잠금, 화면 종료"]
        AA -- 아니요 --> AC["✏️ 다음 답변 입력 대기"]
    end

    style STEP1 fill:#1e1e2e,stroke:#cba6f7,color:#cdd6f4
    style STEP2 fill:#1e1e2e,stroke:#89b4fa,color:#cdd6f4
    style STEP3 fill:#1e1e2e,stroke:#a6e3a1,color:#cdd6f4
    style STEP4 fill:#1e1e2e,stroke:#f38ba8,color:#cdd6f4
    style STEP5 fill:#1e1e2e,stroke:#fab387,color:#cdd6f4
    style STEP6 fill:#1e1e2e,stroke:#f9e2af,color:#cdd6f4

    style R fill:#5c1a1a,stroke:#f38ba8,color:#fff
    style S fill:#3d3a00,stroke:#f9e2af,color:#fff
    style T fill:#0d3320,stroke:#a6e3a1,color:#fff
```

---

## 체셔가 힌트를 어떻게 조절하나요?

체셔는 플레이어를 **계속 지켜보면서** 세 가지를 측정합니다.

| 측정 항목 | 의미 |
|----------|------|
| **진행도** | 지금까지 퀴즈를 얼마나 풀었는가 |
| **정답률** | 맞힌 문제가 얼마나 많은가 |
| **막힘 정도** | 같은 방을 자꾸 들락날락하거나 오랫동안 진전이 없는가 |

이 세 가지를 종합해서 **실력 점수**를 계산하고, 점수에 따라 다음처럼 힌트를 다르게 줍니다.

---

## 힌트 수준 비교

```mermaid
flowchart LR
    A["😰 많이 막힌 플레이어\n실력 점수 낮음"] --> B["🟥 직접 힌트\n다음에 뭘 해야 하는지\n구체적으로 알려줌\n단, 정답 자체는 말하지 않음"]

    C["😐 보통 플레이어\n실력 점수 중간"] --> D["🟨 방향 힌트\n어느 쪽으로 생각해야\n하는지만 알려줌"]

    E["😎 잘 하는 플레이어\n실력 점수 높음"] --> F["🟩 최소 힌트\n거의 알려주지 않고\n스스로 탐색하도록 유도"]

    style A fill:#1e1e2e,stroke:#f38ba8,color:#cdd6f4
    style B fill:#5c1a1a,stroke:#f38ba8,color:#fff
    style C fill:#1e1e2e,stroke:#f9e2af,color:#cdd6f4
    style D fill:#3d3a00,stroke:#f9e2af,color:#fff
    style E fill:#1e1e2e,stroke:#a6e3a1,color:#cdd6f4
    style F fill:#0d3320,stroke:#a6e3a1,color:#fff
```

---

## 막힘 점수는 어떻게 올라가나요?

```mermaid
flowchart TD
    A["플레이어 행동 관찰"] --> B["같은 방에\n자꾸 돌아오는가?\n전체 막힘 점수의 60%"]
    A --> C["너무 짧은 간격으로\n다시 오는가?\n45초 기준\n전체 막힘 점수의 25%"]
    A --> D["와도 아무 진전이\n없는가?\n전체 막힘 점수의 15%"]
    B & C & D --> E["막힘 점수 합산"]
    E --> F["실력 점수에 반영\n막힘 점수가 높을수록\n힌트를 더 많이 줌"]
```

---

## 체셔와 대화하는 순서 요약

> 1. **창문 클릭** → 체셔 창이 열림
> 2. **앵무새 클릭** → 퀴즈 시작 (이 순서를 지켜야 함)
> 3. **체셔가 질문** → 플레이어 실력에 맞는 힌트 수준으로 출제
> 4. **플레이어가 답변 입력** → 채점 서버에서 정답 여부 판단
> 5. **체셔가 반응** → 정답이면 다음 문제, 오답이면 다시 도전
> 6. **5문제 완료** → 퀴즈 종료
